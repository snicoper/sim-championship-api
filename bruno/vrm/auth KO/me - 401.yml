info:
  name: me - 401
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/auth/me"

runtime:
  scripts:
    - type: tests
      code: |-
        const data = res.getBody();

        test("Should me Unauthorized", () => {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
