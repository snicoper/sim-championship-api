info:
  name: reset-password - 409
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "token": "{{verificationToken}}",
        "password": "{{userPassword}}",
        "confirmPassword": "{{userPassword}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should reset password conflict", () => {
          expect(res.getStatus()).to.equal(409);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
