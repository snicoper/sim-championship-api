info:
  name: me - 200
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/auth/me"

runtime:
  scripts:
    - type: tests
      code: |-
        const data = res.getBody();

        test("Should me successfully", () => {
          expect(res.getStatus()).to.equal(200);
        });

        test("Should return authenticated user profile", () => {
          expect(data.id).to.be.a("string");
          expect(data.email).to.equal(bru.getVar("registerEmail"));
          expect(data.isActive).to.be.true;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
