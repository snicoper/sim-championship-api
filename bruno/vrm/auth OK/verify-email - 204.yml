info:
  name: verify-email - 204
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/verify-email"
  body:
    type: json
    data: |-
      {
        "token": "{{verificationToken}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should verify email successfully", () => {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
