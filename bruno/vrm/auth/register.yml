info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{base-url}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "{{registerEmail}}",
        "password": "Password123!",
        "confirmPassword": "Password123!"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const timestamp = Date.now();
        const random = Math.floor(Math.random() * 10000);

        bru.setVar('registerEmail', `test${timestamp}${random}@example.com`);
    - type: after-response
      code: |-
        const data = res.getBody();

        bru.setVar(
          'verificationToken',
          data.verificationToken,
        );
    - type: tests
      code: |-
        const data = res.getBody();

        test("Should return verification token after successful registration", () => {
          expect(res.getStatus()).to.equal(201);

          expect(data.verificationToken).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
