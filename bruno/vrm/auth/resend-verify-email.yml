info:
  name: resend-verify-email
  type: http
  seq: 2

http:
  method: POST
  url: "{{base-url}}/auth/resend-verify-email"
  body:
    type: json
    data: |-
      {
        "email": "{{registerEmail}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should resend verify email successfully", () => {
          expect(res.getStatus()).to.equal(201);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
