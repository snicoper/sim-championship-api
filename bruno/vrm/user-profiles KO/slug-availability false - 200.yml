info:
  name: "slug-availability false - 200 "
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/user-profiles/slug-availability/{{userProfileSlug}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should slug-availability 200", () => {
          expect(res.getStatus()).to.equal(200);
        });

        test("Should slug-availability return isAvailable false", () => {
          const data = res.getBody();
          expect(data.isAvailable).to.equal(false);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
