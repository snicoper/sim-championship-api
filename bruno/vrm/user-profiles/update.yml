info:
  name: update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base-url}}/user-profiles"
  body:
    type: json
    data: |-
      {
        "nickname": "A.Test",
        "firstName": "Amen",
        "lastName": "Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
